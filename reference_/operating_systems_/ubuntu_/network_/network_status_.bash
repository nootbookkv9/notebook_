# Network status
	##############################
	# Choice 1 ###################
		$ sudo apt install nethogs
		$ sudo nethogs

	##############################
	# Choice 2 ###################
		$ sudo apt install iftop
		$ sudo iftop -i wlp2s0
		$ sudo iftop -i ??WiFi-name

	##############################
	# Choice 3 ###################
		$ sudo apt install nload
		$ nload

