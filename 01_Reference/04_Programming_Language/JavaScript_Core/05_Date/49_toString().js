// Convert a date object to a string:
let d = new Date();
let text = d.toString();
console.log('text', text);
//: Sat Oct 19 2024 21:51:18 GMT+0100 (Central European Standard Time)

// Description:
// The toString() method returns a date object as a string.

// Note:
// Every JavaScript object has a toString() method.
// The toString() method is used internally by JavaScript when an object needs to be displayed as a text (like in HTML), or when an object needs to be used as a string.
// Normally, you will not use it in your own code.

// Syntax:
// Date.toString()

// Parameters:
// NONE

// Return:
// Date and time represented as a string

// History:
// toString() is an ECMAScript1 (ES1) feature.
// ES1 (JavaScript 1997) is fully supported in all browsers:
